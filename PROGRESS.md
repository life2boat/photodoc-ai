📜 Журнал разработки: PhotoDoc AI
🗓 26 апреля 2026 г. (Спринт: Инфраструктура и Интеграция)
🏗 Инфраструктура (Infrastructure Path)
[x] Глобальный MCP: Настроена глобальная среда в ~/.gemini/settings.json. Подключены серверы context7 (документация) и filesystem (безопасный доступ к диску E:).

[x] Конституция проекта: Создан GEMINI.md. В нем зафиксированы стек (React/FastAPI/Supabase), правила безопасности (запрет на хардкод ключей) и протоколы взаимодействия с ИИ.

[x] Стандарты UI: Создан DEVTOOLS-CHECKLIST.md. Теперь агент знает требования к адаптивности (iPhone 12/390px), контрастности и UX.

[x] QA-автоматизация: Установлен и инициализирован Playwright. Проведены первые успешные тесты (6/6 passed). Сформирован deploy.yml для GitHub Actions.

[x] Playbook: Составлена инструкция по быстрому развертыванию этой среды на новом рабочем месте.

🚀 Продукт и Фичи (Product Path)
[x] Баг-фикс калькулятора: Восстановлена категория «Реставрация фото» с актуальными ценами:

Легкая: 200 ₽

Глубокая (ИИ): 350 ₽

Колоризация: 150 ₽

[x] Бэкенд (Supabase): - Спроектирована и создана таблица orders в облаке.

Настроены поля: client_name, client_phone, category, service_name, quantity, total_price, status.

Отключен RLS для прямого доступа фронтенда.

[x] Интеграция: - Установлен @supabase/supabase-js.

Создан клиент src/lib/supabase.js.

Реализована функция handleSubmit в калькуляторе. Заявки теперь улетают в реальную базу.

🛠 Текущий статус
Локальный адрес: http://localhost:5173/photodoc-ai/

База данных: Таблица orders активна и принимает данные.

Безопасность: Все ключи вынесены в .env.
🛠 Стандарт: Конвейер загрузки файлов (PhotoDoc AI Core)
Этот протокол обязателен для всех виджетов, предполагающих отправку файлов на сервер.

1. Фронтенд (React / Client Side)
Метод отправки: Использование объекта FormData.

Заголовки (Headers): Категорический запрет на ручную установку Content-Type. Браузер должен автоматически выставлять multipart/form-data с корректным boundary.

Управление состоянием: * Файлы хранятся в массиве selectedFiles.

После успешного ответа сервера (response.ok) выполняется полная очистка состояния: setSelectedFiles([]).

UI принудительно обновляется через updateGallery() или аналогичную функцию для очистки превью.

Безопасность: Блокировка стандартного поведения браузера для Drag-and-Drop на уровне window, чтобы избежать случайного открытия фото в новой вкладке.

2. Бэкенд (FastAPI / Server Side)
Эндпоинт: Единый унифицированный путь /api/order.

Хранение: * Корневая папка: uploads/.

Изоляция: Создание уникальной папки под каждый заказ (Order_YYYYMMDD_HHMMSS или порядковый номер).

Метаданные: Параллельно с сохранением фото сервер обязан генерировать файл order_info.txt со всеми деталями заказа (ФИО, телефон, состав услуг, итоговая стоимость).

3. Администрирование (Admin Panel)
Сборка заказа: Использование io.BytesIO для создания ZIP-архива.

Ресурсосбережение: Архивация происходит исключительно в оперативной памяти «на лету» в момент запроса. Физические ZIP-файлы на диске сервера не создаются.

Почему это важно (Reminder):
Масштабируемость: Мы можем добавить 10 новых типов услуг, не меняя логику обработки файлов.

Стабильность: Этот метод исключает ошибки 422 (неверный формат данных) и 404 (рассинхрон путей).

Порядок: Оператор всегда получает структурированный архив, где фото и описание лежат вместе.
29.04.2026] Итоги дня: Настройка Full-stack ядра и подготовка к деплою
✅ Выполненные задачи
Поднят и отлажен локальный бэкенд (FastAPI): Разрешены конфликты путей запуска модуля, собрано чистое виртуальное окружение с установкой всех необходимых зависимостей (python-dotenv, jinja2).

Завершен сквозной тест (End-to-End): Успешно протестирована связка React-фронтенда и FastAPI-бэкенда. Форма с фотографией и данными клиента корректно уходит с виджета реставрации, обрабатывается сервером и сохраняется в локальную БД SQLite и файловую систему (/uploads).

Утверждена архитектура деплоя: Принято решение использовать VPS в качестве шлюза для приема заказов 24/7 с последующей бесшовной синхронизацией файлов на рабочий ПК через Syncthing и уведомлениями по Email.

Оптимизация кодовой базы: Проведен рефакторинг backend/main.py. Полностью вырезан модуль ИИ-ассистента (OpenAI) для облегчения бэкенда и устранения зависимости от внешних API-ключей при старте.

Интеграция Email-овещений: Внедрена фоновая задача (BackgroundTasks) для отправки писем с деталями заказа через SMTP (Яндекс), чтобы не замедлять отдачу ответа клиенту на фронтенде.

🐛 Исправленные баги
Устранен синтаксический конфликт экспорта/импорта компонента RestoreWidget.jsx в React.

Исправлена ошибка маршрутизации 404 Not Found (эндпоинты фронтенда и бэкенда приведены к единому стандарту /api/order).

Устранен краш бэкенда при инициализации, связанный со старым кодом OpenAI.

📌 План на следующий спринт (Деплой)
Сделать бэкап VPS (снимок сервера перед работами).

Настроить Nginx и пробросить Fallback-маршрутизацию в панели, чтобы сайт работал параллельно с текущим Xray (VLESS+Reality) на 443 порту без конфликтов.

Задеплоить проект (перенести сбилженный фронтенд и бэкенд на сервер).

Настроить Syncthing для автоматической перекачки папки uploads с VPS на жесткий диск.

(В планах) Интеграция эквайринга (страница оплаты).
Отчет о прогрессе: PhotoDoc AI (Core Order Flow)
🎯 Фокус этапа
Полная автоматизация воронки оформления заказа: от клика пользователя на фронтенде до логичного распределения файлов в облаке и маршрутизации уведомлений.

✅ Выполнено
Backend (FastAPI & Интеграции)
Умная маршрутизация файлов: Настроено автоматическое создание вложенных директорий на Яндекс.Диске через API. Файлы раскладываются по структуре: Заказ_ID_Имя / Услуга_Формат_Бумага.

Генерация метаданных: Реализовано автоматическое создание файла info.txt внутри папки заказа, содержащего контактные данные клиента и технические требования к печати.

Уведомления: Стабилизирована отправка email-уведомлений через SMTP с использованием безопасных паролей приложений (App Passwords).

Оптимизация работы оператора: В шаблон email-уведомления добавлена прямая безопасная ссылка (URL-encoded) на веб-интерфейс Яндекс.Диска для моментального перехода к файлам заказа в один клик.

Frontend (React)
Управление состоянием (State Management): Устранен критический баг с нативной перезагрузкой страницы при сабмите формы. Данные теперь отправляются асинхронно (через fetch), состояние UI сохраняется.

Улучшение UX экрана успеха: Настроено корректное отображение экрана "Ваш заказ сформирован". Реализовано условное скрытие блока "Умный калькулятор" при успешном заказе, что сделало интерфейс чистым и акцентированным на целевом действии (оплате).

Унификация компонентов: Логика отправки данных в компонентах стандартной фотопечати и Polaroid приведена к единому стандарту бэкенда (массив files + обязательные текстовые поля).

DevOps & Безопасность
Аудит безопасности (Secret Scanning): Оперативно разрешен инцидент с блокировкой пуша от GitHub. Токены доступа (Personal Access Token) удалены из истории коммитов.

Git Policy: Обновлены правила .gitignore (добавлен игнор .vscode/ и локальных конфигураций), проведена очистка кэша Git для предотвращения будущих утечек.

🚀 Следующие шаги (Бэклог)
[ ] Интеграция платежного шлюза (эквайринг) к кнопке «Перейти к оплате».
🚀 Прогресс проекта: PhotoDoc AI (Финансовое ядро и Логистика)
✅ 1. База данных и Бэкенд (FastAPI + SQLite)
Расширена схема БД: В таблицу заказов (orders.db) добавлены критически важные поля: email, format, paper, crop и payment_email_sent_at. Бэкенд теперь «помнит» все детали заказа после возврата пользователя с оплаты.

Надежная генерация платежей: Сумма заказа (amount) теперь строго форматируется через Decimal (например, 40.00), что исключает ошибки плавающей точки. Параметр IsTest жестко передается как "1".

Валидация подписей (Security): Настроена строгая проверка MD5-хэшей от Робокассы. Для инициализации и страницы успеха (Success URL) используется PASSWORD1, для фонового подтверждения (Result URL) — PASSWORD2.

✅ 2. Интеграция с Робокассой (UX)
Бесшовный переход: В ссылку на оплату теперь автоматически прокидываются параметры OutSum (зафиксированная сумма) и Email.

Улучшение UX: Клиенту больше не нужно дважды вводить свою почту — форма Робокассы подхватывает её автоматически и скрывает лишнее поле.

✅ 3. Фронтенд (React/Vite)
Синхронизация данных: Во все ключевые виджеты (PhotoPrintForm, RestoreWidget, IdPhotoWidget, PolaroidWidget) добавлена сборка объекта FormData, которая гарантированно передает email клиента и параметры заказа (format, paper, crop, comment) на бэкенд.

✅ 4. Логистика файлов (Яндекс.Диск)
Полная автоматизация: Бэкенд успешно создает уникальную папку под каждый заказ (например, PhotoDoc_Orders/Заказ_31_имя).

Генерация лога: Вместе с фотографиями на диск автоматически загружается сформированный файл info.txt со всеми характеристиками заказа для производства. Ошибки 409 Conflict (папка уже существует) корректно обрабатываются.

✅ 5. Почтовые уведомления (SMTP Яндекс)
Разделение потоков: Логика писем разбита на два независимых этапа:

Мгновенное письмо Администратору: Уходит сразу при нажатии кнопки «Оплатить». Содержит все данные клиента и пути к файлам на Яндекс.Диске. Гарантирует, что ни один заказ не потеряется.

Письмо-подтверждение Клиенту: Уходит только после успешного подтверждения оплаты от Робокассы.

Защита от спам-фильтров: Заголовок From жестко привязан к SMTP_USER, что решило проблему блокировки писем почтовыми серверами Яндекса. Добавлена защита от дублирования писем.

🚧 Что осталось сделать (Текущий статус):
Код полностью готов и отлажен. * Локальная проблема с вебхуками: В данный момент Робокасса после оплаты пытается отправить подтверждение (Result URL) на боевой домен http://photodoc-ai.ru/ (или браузер пытается туда перейти), минуя наш локальный туннель ngrok.

Решение: Либо прописать актуальные ссылки ngrok в кабинете Робокассы для тестового режима, либо переходить к деплою (развертыванию) проекта на боевой сервер, где домен photodoc-ai.ru будет работать штатно, а SSL-сертификаты уберут ошибку Mixed Content.